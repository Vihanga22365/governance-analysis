# MCP Server Environment Variables

# Backend API Configuration
# Replace 'localhost' with your server's IP address or domain name when deploying
BACKEND_HOST=localhost
BACKEND_PORT=8353

# Examples:
# Local development:
# BACKEND_HOST=localhost

# Network access (same network):
# BACKEND_HOST=192.168.1.100

# Production server:
# BACKEND_HOST=194.163.140.73
# or
# BACKEND_HOST=api.yourdomain.com
